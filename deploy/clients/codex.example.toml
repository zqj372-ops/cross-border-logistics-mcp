template_status = "示意模板/待管理员验证"
client = "codex"
client_id = "client_demo"
tenant_id = "tenant_demo"
transport = "streamable-http"
endpoint = "https://mcp.example.invalid/mcp"
token_source = "enterprise_identity_provider"
token = "INJECT_FROM_IDENTITY_PROVIDER"
tools = [
  "cargo.calculate",
  "container.plan_summary",
  "quote.canada_final_mile.calculate",
  "quote.save_draft",
  "customs.ca.search",
  "customs.ca.estimate",
  "knowledge.search_curated",
  "system.get_data_status",
  "review.create_task",
]
[state_policy]
success = "render versioned data and trace"
needs_input = "ask only for listed blockers"
manual_review = "handoff with reason, source and owner"
unavailable = "show unavailable; do not substitute a value"
blocked = "show policy denial; do not retry by another tool"
sendable = false
theoretical_only = true
status_handling = "See docs/runbooks/client-onboarding.md; success remains sendable=false and theoretical_only=true where declared."
