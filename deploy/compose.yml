services:
  logistics-mcp:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    image: cross-border-logistics-mcp:candidate
    restart: "no"
    expose:
      - "8080"
    environment:
      MCP_PORT: "8080"
      MCP_DATA_MODE: "${MCP_DATA_MODE:-production}"
      MCP_JWT_ISSUER: "${MCP_JWT_ISSUER:-https://issuer.example.invalid/}"
      MCP_JWT_AUDIENCE: "${MCP_JWT_AUDIENCE:-logistics-mcp-demo}"
      MCP_ALLOWED_ORIGINS: "${MCP_ALLOWED_ORIGINS:-https://client.example.invalid}"
      MCP_ALLOWED_HOSTS: "${MCP_ALLOWED_HOSTS:-mcp.example.invalid}"
      MCP_ALLOWED_OUTBOUND_HOSTS: "${MCP_ALLOWED_OUTBOUND_HOSTS:-riskcustoms.example.invalid,quote.example.invalid}"
      MCP_TENANT_MAPPING: "${MCP_TENANT_MAPPING:-tenant_demo}"
      MCP_CLIENT_ID: "${MCP_CLIENT_ID:-client_demo}"
      MCP_UPSTREAM_TOKEN_FILE: "/run/secrets/mcp_upstream_token"
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=16m
    user: "10001:10001"
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD
        - node
        - --input-type=module
        - -e
        - fetch('http://127.0.0.1:8080/healthz').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    secrets:
      - mcp_upstream_token

secrets:
  mcp_upstream_token:
    external: true
