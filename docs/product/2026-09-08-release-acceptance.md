# 本轮业务上线验收记录

日期：2026-09-08。当前结论：代码候选可验收，正式业务上线未通过。

## 已有证据

- 线上源运价 754 档、有效城市规则 2,011 行已完整迁入隔离验收库。每条规则运行结果为成功 1,279 行、源分区停用 728 行、同城冲突 4 行；成功结果的分区及基础价格均与源表一致。
- Toronto / Calgary 的网页与人员 CLI 发布读回一致，运价截止 2027-01-01，报价有效期 7 天。正式报价单日期仍须受运价截止日限制。
- 隔离浏览器流程通过混装、原生保存、CLI 查询、退回、审核和 PDF 导出，桌面与手机没有横向溢出。承运商请求验证了两托实体货物和交付条件；没有真实承运商费率成功回执。
- 目标 Oracle 候选容器已能在 Chromium 沙箱开启时生成中文 PDF 样张。候选构建及样张不是正式客户报价验收。

## 关务发布缺口（直接读取来源记录）

运行源数据仍为 15 个 staged release，publication_snapshot 为 0。源归档检查 20 项中 19 项通过；旧版 ca-sima-details-2026-08-14 缺有效归档引用。

本机恢复目录中 10 个规范化包均为 candidate，没有审核者/审批编号：

| 规范化包 | 质量报告 | 仍缺的发布证据 |
| --- | --- | --- |
| 美国 HTS 2026 r18 | pass；包含章节 98 / 99 覆盖计数 | reviewed 状态、审批、与 r16 的复核差异及附加税矩阵证据 |
| 加拿大 t2026-2 英文 | review；42 项 exclusions | 排除项核对及质量通过、审批 |
| 加拿大 t2026-2 法文 | review；41 项 exclusions，税率行 0 | 法文法律内容覆盖的核对及质量通过、审批 |
| 中国税则、商品编码、出口税率、出口许可证、两用物项、对加调整，以及加拿大 SIMA 2026-09-05（7 包） | 未见 quality-report.json | 完整质量报告、reviewed 状态和审批 |

上述是文件与运行元数据核验，不是对当前法律条文有效性的认定。候选 coverage gate 还缺高风险规则、美国附加税交互矩阵、加拿大法文法律数据和最新官方来源检查的通过证据。不能通过填写假审批或直接更新数据库状态来解除。

用户已于本轮明确授权发布关务正式快照。授权已具备，但数据检查仍未通过，不应重复询问一般发布许可。

进一步读取恢复的 `interaction-candidate.json` 后，确认美国附加税包仍为 `manual_review`：缺完整行动清单、国家与原产地绑定、商品包含/排除项、后续修订检查、税项间叠加/不叠加规则，以及 AD/CVD 案件、出口商与当前保证金税率绑定。这些是候选包实际声明的缺项，不能仅补审批字段解除。恢复的监管来源审计虽有 19 项成功抓取，也明确不等于法律适用范围已审核。当前全量发布合同仍要求这些检查通过；按基础税率分项发布需要另行明确范围和失败闭合行为。

## 正式配置缺口

- 已按用户新指示改为企业自行填写公司资料，平台不预设出单公司；这不再作为整个平台上线前置条件。仅当前企业首次制作报价单前需要填写其公司名称和条款。网页企业模板与 `workspace documents config-save` 共用配置，历史单据保留原快照。
- 运行中的 Portal 企业连接未配置 Freightcom 能力；本轮没有可证明该企业真实承运商询价成功的回执。凭证需通过后台安全录入，不写进文档、Git 或聊天。
- H8Y / PIERREFONDS 的 Zone 7/8 与 J0Y / ROUYN-NORANDA 的 Zone 8/9 保留人工复核。其他源规则可正常按发布开关使用。

## 公司自助配置验收

- 本地 `http://127.0.0.1:8907/console/`：报价单空白页 → 填写公司与报价模板 → 企业模板 → 服务市场，交互通过；公司名称与条款保持空白。
- 桌面 1440×1000、手机 390×844 显示正常，无横向溢出或脚本异常。测试环境匿名关税额度接口返回 HTTP 503 / `public_customs_unavailable`，未将该响应忽略为服务就绪。
- 报价单与人员 CLI 定向测试 4 文件 / 16 测试通过；应用和 CLI 构建、页面静态检查及差异格式检查通过。本次只调整页面说明与上线文档，沿用已有企业隔离和模板版本行为。

## 发布边界

代码合并和生产切换分别记录。当前运行中的生产 Portal 未切换，生产业务数据库迁移尚未执行；本地隔离配置、合成报价及测试身份不会复制到正式企业。正式企业配置和上述业务验收完成后，再按运行手册备份、迁移、切换及读回。
